{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "dogu-config-schema",
  "description": "Schema for the dogu.config.json file",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "[optional] Schema for the dogu.config.json file",
      "type": "string"
    },
    "version": {
      "description": "[optional] Version of the dogu.config.json schema",
      "type": "integer"
    },
    "apiBaseUrl": {
      "description": "[required] Base URL of the dogu's API",
      "type": "string"
    },
    "token": {
      "description": "[required] Token to be used for dogu API calls",
      "type": "string"
    },
    "organizationId": {
      "description": "[required] Organization ID to be used for dogu API calls",
      "type": "string"
    },
    "projectId": {
      "description": "[required] Project ID to be used for dogu API calls",
      "type": "string"
    },
    "runsOn": {
      "description": "[required] tag of the environment the dogu is running on",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "uniqueItems": true
        }
      ]
    },
    "browserName": {
      "description": "[optional] Name of the browser to be used for testing",
      "type": "string"
    },
    "browserVersion": {
      "description": "[optional] Version of the browser to be used for testing",
      "type": "string"
    }
  },
  "required": ["apiBaseUrl", "token", "organizationId", "projectId", "runsOn"]
}
