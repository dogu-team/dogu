import { WebDriverSessionId } from '@dogu-private/types';

export interface CreateRemoteWebDriverInfoSessionDtoBase {
  sessionId: WebDriverSessionId;
}

export interface UpdateRemoteWebDriverInfoSessionDtoBase {
  sessionId: WebDriverSessionId;
}
