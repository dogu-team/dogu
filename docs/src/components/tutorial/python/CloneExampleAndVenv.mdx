import CodeBlock from '@theme/CodeBlock';

<h3>{props.title}</h3>

<CodeBlock language="shell">
  {`git clone https://github.com/dogu-team/${props.repo_name}.git
cd ${props.repo_name}/${props.path}
  
python3 -m venv .venv
`}
</CodeBlock>
