---
sidebar_position: 2
title: Generate GitHub Token
locale: en
---

## Fine-grained token (recommended)

1. Log in to [GitHub](https://github.com) or self-hosted GitHub.
2. Go to Profile > Settings > Developer settings > Personal access tokens in the header.
3. Click on the Fine-grained tokens tab.
4. Click on the "Generate new token" button.
   <img
     src="/img/console/project/github-fine-grained-token-gen.png"
     style={{ width: 500, display: 'block' }}
   />
5. Set the token name and expiration date.
6. In the Resource owner menu, select the organization.
   <img
     src="/img/console/project/github-fine-grained-token-name.png"
     style={{ width: 500, display: 'block' }}
   />
7. Enter the appropriate Repository access.
   - It is recommended to select "Only select repository".
     <img
       src="/img/console/project/github-fine-grained-token-repos.png"
       style={{ width: 500, display: 'block' }}
     />
8. In the Repository permissions, set the `Content` item to `read-only` or higher.
   <img
     src="/img/console/project/github-fine-grained-token-perm.png"
     style={{ width: 500, display: 'block' }}
   />
9. Click on the "Generate token" button.

## Classic token

1. Log in to [GitHub](https://github.com) or Self-Hosted GitHub.
2. Go to Profile > Settings > Developer settings > Personal access tokens.
3. Click on the Tokens (classic) tab.
4. Click on the Generate new token (classic) button.
   <img
     src="/img/console/project/github-classic-token-gen.png"
     style={{ width: 500, display: 'block' }}
   />
5. Set the token name and expiration date.
   <img
     src="/img/console/project/github-classic-token-name.png"
     style={{ width: 500, display: 'block' }}
   />
6. Select the scope `repo`.
   <img
     src="/img/console/project/github-classic-token-perm.png"
     style={{ width: 500, display: 'block' }}
   />
7. Click on the Generate token button.
