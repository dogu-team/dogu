{
  "verbose": true,
  "watch": ["./public", "./electron", "../../mirror/packages"],
  "exec": "yarn install && yarn build && concurrently \"cross-env BROWSER=none yarn start\" \"wait-on http://127.0.0.1:3333 && electron .\"",
  "ext": "ts,tsx,js,json",
  "delay": 3000
}
